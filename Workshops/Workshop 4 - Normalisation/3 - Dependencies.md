# Functional Dependencies

## What Are Functional Dependencies?
A functional dependency occurs when one attribute uniquely determines another.

### Example
If `CustomerID` determines `CustomerName`, then `CustomerName` is functionally dependent on `CustomerID`.

---

_Next topic: [First Normal Form](./first_normal_form.md)_
